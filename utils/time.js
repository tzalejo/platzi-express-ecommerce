// Para listado solo tendra un cache de 5 minutos y para el detalle(dependiendo d la logica de desarrollo) sera por 60ming
const FIVE_MINUTES_IN_SECONDS =300;
const SIXTY_MINUTES_IN_SECONDS =3600;

module.exports = {
  FIVE_MINUTES_IN_SECONDS,SIXTY_MINUTES_IN_SECONDS
};
